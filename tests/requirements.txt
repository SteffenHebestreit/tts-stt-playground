pytest>=7.0
pytest-asyncio>=0.21
pytest-cov>=4.1

# These four must MATCH frontend-service/requirements.txt, not merely satisfy
# it. The unit tests import frontend-service/app.py directly, so a floating
# `fastapi>=0.104` meant CI exercised whatever FastAPI happened to be current
# while the container shipped the pinned one — two different frameworks under
# the same test name. That drift is not theoretical: it resolved to Starlette
# 1.x, which removed the legacy TemplateResponse(name, context) signature, so
# CI failed on a code path that worked perfectly in the image.
#
# Keep these in lockstep with frontend-service/requirements.txt.
fastapi==0.115.8
jinja2==3.1.5
python-multipart==0.0.20
httpx==0.28.1

# Needed by the offline stt-service WebSocket tests, which stub torch and
# faster-whisper but exercise the real numpy buffer handling.
numpy>=1.24
